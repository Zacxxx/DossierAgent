# Packages

This directory is the monorepo package root. Each child directory is a package with its own README, package metadata, source folder, and tests.

Package-level dependencies must stay local to the package. Cross-package behavior is composed from `packages/core`.

