[project]
name = "dossieragent-search-engine"
version = "0.1.0"
description = "Elasticsearch indexing and search package for DossierAgent."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

